# Normalize line endings: LF in the repo for everything text.
* text=auto eol=lf

# Windows-only helper scripts keep CRLF on checkout.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# Binary assets — never touch.
*.png binary
*.ico binary
